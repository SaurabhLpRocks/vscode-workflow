version: '3.4'

services:
  core.api.new.service:
    image: core.api.new.prod.image
    container_name: core.api.new.prod.container
    build:
      context: .
      dockerfile: .docker/production.dockerfile
    ports:
      - 5011:80
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
